# Tutorial Update Checklist

Use this checklist at the end of every milestone before the milestone is considered fully closed.

Target tutorial:

- `docs/project_total_walkthrough_obsidian.md`
- `docs/modeling_tutorial_for_materials_undergrads.md`
- `docs/python_code_teaching_for_beginners.md`

Required updates:

1. Add or revise the milestone summary in the tutorial.
2. Update the code-structure section if new files or entry points were added.
3. Update the physics section if new governing equations, assumptions, or boundary conditions were introduced.
4. Update the numerical-method section if the solver, discretization, thresholds, or validation logic changed.
5. Update the interpretation section if the new results changed how earlier figures should be read.
6. Update the limitations section if a previous limitation was removed or a new limitation became important.
7. Update the references section if new papers or data sources were used.

Minimum expected output after each milestone:

1. The milestone report exists.
2. The main tutorial is refreshed.
3. The tutorial and the milestone report do not contradict each other.

## Tutorial Sync Reminder

- Treat `docs/tutorial_update_checklist.md` as part of the default milestone closeout checklist.
- After every milestone report, verify that `docs/project_total_walkthrough_obsidian.md` has been refreshed before marking the milestone fully closed.
